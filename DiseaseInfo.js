import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const DiseaseInfo = (name) => {
   return (
      <View>
         <Text>DiseaseInfo</Text>
         <Text>{name}</Text>
      </View>
   )
}

export default DiseaseInfo

const styles = StyleSheet.create({})